import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  smMargin: {
    // margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: 'center',
  },
}));